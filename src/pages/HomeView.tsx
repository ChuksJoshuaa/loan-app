import { Header, HeroSection } from "../components";

const HomeView = () => {
  return (
    <div className="main-container">
      <Header type="home" />
      <HeroSection />
    </div>
  );
};

export default HomeView;
